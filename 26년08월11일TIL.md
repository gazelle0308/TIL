# TIL
#### 8월 11일 


알고리즘 스터디 :
-
    1. 개요
     작성자는 임베디드 및 기초적인 C언어 학습을 중심으로 전공하였기에 하드 코딩 중심의 프로그래밍이 중심적이다.

     그러하다 보니 하드 코딩으로만 해결하기에는 시간적, 기술적 소모량이 높아, 해결책이 필요하게 되었다.
    
     따라서 작성자는 매일 알고리즘에 대한 코딩 문제를 풀며 알고리즘에 학습하고 이해하며, 사용 가능한 기술의 범위를 확대하기로 하였다.

    2. DP
     DP의 정의: Dynamic Programming의 약자로 큰 문제를 여러 개의 작은 부분 문제로 나누고, 각 부분 문제의 결과를 저장하여 같은 계산을 반복하지 않도록 하면서 전체 문제의 답을 구하는 알고리즘 설계 기법이다.
     
     DP의 장점: 작은 문제의 계산 결과를 저장하고 재사용하여 중복 계산을 줄이고, 누적된 결과를 기반으로 큰 문제의 결과를 효율적으로 계산할 수 있다.

     DP의 단점: 부분 문제의 결과를 저장하기 위한 메모리가 필요하며, 상태의 수와 각 상태를 계산하기 위한 전이 비용이 커지면 시간·공간 복잡도가 증가할 수 있다.

     작성자가 느낀 DP의 가장 특징적인 부분: 역시 가장 특징적인 부분이라고 한다면 문제의 해결과 같이 쌓여가는 Data라는 점이었다. 
     물론 이는 "부분 문제의 결과를 저장하기 위한 메모리가 필요" 라는 단점을 동반하기도 하지만, 달리 말하면 즉각적이고 런타임적으로 메모리의 부하보다 데이터의 누적과 해결책이 필요한 타이밍에 적재적소라는 의미로 받아들여진다. 

    3. 금일 해결한 DP 코딩 문제:

     Link: https://leetcode.com/problems/stone-game-iv?envType=daily-question&envId=2026-08-10 

     code:
     class Solution {
     public:
         bool winnerSquareGame(const int n) {

             int odd = 0;
             int loop = 1;
             int squre = 1;
             int squreIndex = 0;

             std::vector<bool> caseCheck;

             std::vector<int> perfectSquare;

             std::map<int, bool> DPTable;

             if( n == 1 ) {
                 return true;
             }

             DPTable.insert({0, false});
             DPTable.insert({1, true});
             perfectSquare.push_back(1);

             for(loop = 1; squre < n; loop = loop + 1) {
                 odd = 1 + 2 * loop;
                 squre = squre + odd;
                 perfectSquare.push_back(squre);
                 DPTable.insert({squre, true});
             }

             if(n == squre) {
                 return true;
             }

             for(loop = 1; loop <= n; loop = loop + 1) {

                 bool IsWin = false;

                 if(squreIndex < perfectSquare.size() &&
                    loop == perfectSquare[squreIndex]) {
                     squreIndex = squreIndex + 1;
                     continue;
                 }

                 for(int i = 0; 
                     perfectSquare[i] < loop; i = i + 1) {
                     int buf = 0;
                     buf = loop - perfectSquare[i];

                     if(!DPTable.find(buf)->second) {
                         IsWin = true;
                         break;
                     }
                 }
                 DPTable.insert({loop, IsWin});
             }
        
             return DPTable.find(n)->second;
        
         }
     };

    // Case 1: 처음 들어오는 숫자가 제곱수일 경우 앨리스의 승리
    // Case 2: 현재 숫자에서 제곱수를 하나 뺀 결과가 여러 가지일 때,
        // 그중 단 하나라도 앨리스의 패배 상태라면 앨리스는 승리한다.
        //
        // 앨리스가 패배 상태의 숫자를 상대에게 넘길 수 
        // 있다는 것은, 상대가 자신의 턴을 시작할 때부터 패배 상태의 
        // 숫자를 받은 것과 같다.
        //
        // 반대로 모든 경우가 승리 상태라면,
        // 어떤 제곱수를 선택하더라도 상대에게 승리 상태를
        // 넘기게 되므로 앨리스는 패배한다.

                        
        // 로직적 설명
        // x[] = n - perfectSquare[]의 다양한 케이스 에서,
        // x의 요소 중 return false가 존재할 경우 return true,
        // 아니라면 return false



CH3 C++와 UE로 3D 게임 개발: 
-
    1.Actor / Component / Tranform 구조 :
     
      작성자는 금일 강의를 듣다가 Local과 World transform에 대하여 듣게 되었고, 해당 내용을 듣던 와중 "Actor 자체는 SceneComponent처럼 Transform을 직접 가지는 객체가 아니다."라는 내용과 "RootComponent도 Local과 World에 대한 Transform을 가지는가?"라는 의문으로 부터 혼란이 생겨 추가로 조사하였다.

     첫 번째 조사:
     - Actor 자체는 SceneComponent처럼 Transform을 직접 가지는 객체가 아니다.
     (Actor가 월드에 존재하더라도 USceneComponent처럼 Transform을 직접 보유하는 구조는 아니다.)


     - RootComponent인 SceneComponent가 Actor의 공간적 기준을 담당한다.
     (그래서 RootComponent가 있으면 Actor의 위치/회전/스케일을 RootComponent를 통해 표현하고 조작할 수 있다.)


     - 일반적인 RootComponent는 부모 SceneComponent가 없기 때문에 Relative와 World Transform이 사실상 1:1로 대응한다.
     (즉 Root에서는 둘을 복잡하게 구분해서 생각할 필요가 거의 없다.)

     # 따라서 실사용 관점에서는 RootComponent의 Transform ≈ Actor의 Transform이라고 생각해도 된다.

    가 첫번 째 결론이었다.
    하지만 그 후  
    
     - 일반적인 RootComponent는 부모 SceneComponent가 없기 때문에 Relative와 World Transform이 사실상 1:1로 대응한다.
    
    를 보며 "혹시 RootComponent가 부모 SceneComponent를 가지고 있을 수 있는 경우도 있지 않을까?" 하여 추가 조사한 결과

     - Actor 자체가 다른 SceneComponent에 Attach되는 경우,해당 Actor의 RootComponent에도 AttachParent가 설정될 수 있다.
     이 경우 RootComponent의 Relative Transform은 부모 SceneComponent를 기준으로 하며,
     World Transform은 부모의 World Transform과 RootComponent의 Relative Transform을 조합하여 결정된다.
     (따라서 이 경우에는 RootComponent에서도 Relative와 World Transform을 구분해야 한다.)

    라는 내용까지 알 수 있었다.

    PS. 이해가 아니라 암기 해야하는 용어: Roll(X회전), pitch(Y회전), yaw(Z회전)