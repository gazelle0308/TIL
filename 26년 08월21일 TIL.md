# TIL
## 8월 21일 

### 1. 자료구조 학습

1. Deque:

    - 내부 구현:
        - 여러 개의 작은 메모리  블럭:
            - 각 메모리 블럭마다 내부적으로 연속되어 있다.
            - 메모리 블럭 끼리는 연속하지 않을 수 있다. 

            (Linked List의 노드처럼 연속되지 않게 존재 할 수 있다.)

            (단 Linked List처럼 서로를 포인터나 참조 등등의 방식으로 연결되지 않을 수 있다.)
        - 관리 범위:
            - 정확히는 현재 `deque`에 들어 있는 원소들의 논리적인 시작 위치(front)와 끝 위치(back)를 추적할 수 있도록 관리한다.
            - 이걸 기반으로 시작과 끝의 내부인지,  몇 번째 메모리 블럭에 어느 인덱스인지 추적하는 형태이다.
        - 확장:
            - 삽입하려는 방향의 여유 공간이 부족하면 새로운 메모리 블럭을 확보하고, 이를 관리 구조에 추가한다.

        상세한 내용은 아래 이미지를 참고하자.

        

        @ 주의 : 
        
        블록 관리 테이블(map)은 Map으로 구현되어 있다는 것이 아니다.
        
        이미지의 화살표에 에러가 존재한다. 해당 내용에 유의가 필요하다.
