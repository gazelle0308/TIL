# TIL
#### 8월 12일 

1.지속적인 알고리즘 풀이를 통해 확인한 나의 강점
-
 - 문제의 핵심을 파악하고 내용을 이해하는 능력은 있는 편이다.
 - 문제에서 요구하는 조건을 이해하고, 직접 부딪히면서 해결 방법을 찾아갈 수 있다. 다만 가장 효율적인 해결책을 처음부터 설계하는 부분에서는 아직 개선 여지가 있다.

 - 한 문제에 집중하면 해결할 때까지 깊게 파고드는 경향이 있다.

    - 장점: 문제의 원리를 제대로 이해할 수 있음

    - 단점: 시간 배분이 무너질 수 있음

2.알고리즘 학습에서 발견한 약점
-
- 인덱스 / 숫자 데이터의 공간적 감각

- 숫자 자체의 관계를 머릿속에서 코드상의 위치로 구현할 때 실수가 발생한다.

    특히:

    - 배열 인덱스
    - 문자열 인덱스
    - 반복문의 시작/종료 위치
    - +1, -1
    - 문자 단위와 바이트 단위
    - 인덱스가 실제 데이터에서 어느 위치를 가리키는지

3.해시 함수
-
해시 함수는 다양한 크기와 형태를 가진 원본 데이터를 고정된 비트 수의 해시값으로 매핑하는 함수이다.

이 고정 크기 해시값은 보통 정수 형태로 다뤄지기 때문에, 원본 데이터를 매번 직접 비교하는 것보다 비교 비용을 줄이고 예측 가능한 크기의 값으로 다룰 수 있다.

특히 문자열이나 구조체처럼 길이나 내부 구성이 다양한 데이터는 비교 비용이 입력 크기에 영향을 받을 수 있지만, 해시값은 32비트, 64비트처럼 고정 폭 값으로 표현되므로 CPU 입장에서 비교와 인덱스 계산을 단순화할 수 있다.

해시 테이블에서는 이 고정 폭 해시값을 기반으로 배열의 저장 위치를 계산한다. 즉, 원본 데이터를 직접 탐색하는 대신 해시값을 이용해 버킷 위치를 빠르게 결정할 수 있다.

이런 점에서 해시는 단순한 변환 함수가 아니라, 가변적인 데이터를 고정 크기의 계산 가능한 값으로 바꾸어 검색·탐색 과정의 비용을 줄이는 알고리즘적 도구라고 볼 수 있다.

다만 해시값은 원본 데이터를 완전히 대체하는 고유값이 아니므로 해시 충돌(Hash Collision)이 발생할 수 있다. 따라서 충돌 처리 방식과 해시 함수의 품질이 실제 성능에 큰 영향을 준다.

PS. 해시값이 충돌하는 경우

예를 들어 아주 단순한 가상의 해시 함수를 생각해 보자.

문자열 내부의 UTF-8 값들을 단순히 더한 값을 해시값으로 사용

이 경우 순서 정보를 고려하지 않는다면:

A + B == B + A

와 같이 서로 다른 입력이 동일한 해시값을 만들 수 있다.

충돌 예시: "초", "ㅜㅊ"

※ 실제 해시 함수가 단순히 UTF-8 값을 더해서 해시값을 만드는 것은 아니며, 위 예시는 해시 충돌의 개념을 설명하기 위한 단순화된 예시이다.

4.오브젝트 폴링
-
- 정의:

    Object Pooling이란, 동적 메모리 할당/해제 자체에 드는 비용과 할당기·메모리 관리 오버헤드, 그리고 반복적인 객체 생성·파괴 비용을 줄이기 위한 기법이다.

    미리 객체와 그에 필요한 메모리를 확보해 둔 뒤,

    SetActorHiddenInGame(true);
    SetActorTickEnabled(false);
    SetActorEnableCollision(false);

    등 객체에서 필요하지 않은 기능을 비활성화하여, 이미 확보한 객체와 메모리를 반복해서 재사용한다.

- 작성자가 보며 느낀 감각:

    작성자는 이 기법이 일종의 트레이드 오프를 이해하고 사용해야 하는 기법으로 느꼈다.

    그도 그러한 것이 미리 객체와 그에 필요한 메모리를 확보해 두는 것을 기반으로 하는 기법이기 때문이다.

    즉 메모리를 실시간으로 일부 확보할 필요가 없는 객체라면 사용할 필요가 없다는 것이며, 또 아직 확인한 적은 없지만 최소화 비용이 너무 적어 생성과 파괴하는 비용이 더 나은 경우에는 사용하지 않을 것으로 보인다.

5.시도해 볼 것
-
1. 프로그래머스 알고리즘 고득점 Kit