# TIL
## 9월 01일 

### 1. 데이터 전 처리

- 목차
    1. Data(데이터)
    2. Raw Data(로우 데이터)
    3. Data Preprocessing의 필요성(데이터 전처리의 필요성)
    4. Raw Data를 목적에 맞는 형태로 변환·정제하는 방법
        1. Initialization(초기 값 설정)
        2. Parsing(파싱)
        3. Padding + Alignment(패딩)
        4. Bit Mask(비트 마스크)
        5. Validation(검증)
        6. Quantization(양자화)
        7. Compression(압축)
        8. Serialization(직렬화)/Deserialization(역직렬화)
        9. Endianness(엔디언)
        10. AoS vs SoA
        11. Normalization

### 1. Data:

저장되어 있거나 신규 생성된 정보를 부르는 단어입니다.

### 2. Raw Data:

가공되지 않은 원본 형태의 정보를 의미하는 데이터의 일종입니다.

`vector`, `deque`, `list`, `map` 등의 자료형에 저장되어 있는지 여부와는 별개로, 데이터가 특정 목적에 맞게 가공·변환되지 않은 원본 상태인지에 따라 Raw Data로 구분할 수 있습니다.

즉, 자료형 자체보다는 데이터를 사용하기 전에 추가적인 해석이나 가공이 필요한 원본 정보인가를 기준으로 Raw Data를 판단합니다.

### 3. Raw Data의 한계:

Raw Data는 원본이기 때문에 그 자체가 우리가 수행하려는 로직의 입력 형태와 일치하지 않을 수 있습니다.

이러한 경우 로직 내부에서 데이터를 처리하고, 검증하고, 필터링하는 과정을 수행하게 되는데, 이렇게 되면 로직의 복잡도가 증가하고 가독성에도 부정적인 영향을 줄 수 있습니다.

- **추가내용:**
    
    > 추후 통신 특강에서 다루겠지만, Raw Data는 손상이 될 수 있습니다. 
    그러한 손상 검증 또한 전 처리와 같은 단계에서 진행합니다.
    (전 처리를 통해 손상 검증을 하기도 하고, 검증 후 실시간 전 처리가 이뤄지기도 합니다.)
    > 
    
    > Raw Data의 "원본"은 절대적인 의미의 근원 데이터가 아니라, 
    특정 처리 단계에서 입력 받은 가공 전 데이터라는 의미입니다.
    > 

### 4. 데이터 전처리의 필요성:

Raw Data의 한계에서 보았던 것처럼
Raw Data는 로직에 적용하기 전에 데이터를 목적에 맞는 형태로 정리할 필요가 있습니다.

### 5. Raw Data를 데이터를 전 처리하는 방법:

데이터 전 처리의 필요성을 인지하였으니, 방법을 알아보겠습니다.

#### **Initialization(초기 값 설정)**

Initialization(초기 값 설정)은 데이터를 사용하기 전에 **초기 상태 또는 초기 값을 설정하는 과정**입니다.

예를 들어보겠습니다.

저희가 풀었던 알고리즘 문제 **「1859. 백만 장자 프로젝트」**의 경우

**"구매 값과 구매 양을 누적하다가, 최대 이윤을 볼 수 있는 날에 판매한다."**가 핵심 로직이었습니다.

이 때 저희가 누적된 구매 금액을 저장하기 위한 변수 `totalBuyPrice`를 선언했다고 생각합시다.

만약 `totalBuyPrice`의 초기 값을 설정하지 않은 상태에서,

```cpp
totalBuyPrice += price;
```

와 같이 값을 누적한다면 어떻게 될까요?

`totalBuyPrice`에 저장된 초기 상태가 보장되지 않기 때문에 정상적인 누적을 수행할 수 없습니다.

따라서 데이터를 처리하기 전에 **데이터를 어떤 초기 상태에서 시작할 것인지 명확하게 설정할 필요가 있습니다.**

이처럼 데이터의 활용과 처리가 정상적으로 이루어질 수 있도록 **사용 전에 초기 상태 또는 초기 값을 설정하는 것을 Initialization(초기 값 설정)**이라고 합니다.

- **추가내용:**
    
    > 추가적인 예시로는 「기능 개발」 문제의 풀이에서 제 풀이의 2번째 풀이 ‘2. deque를 이용한 동적 버퍼 구현’ 부분을 참고할 수 있습니다.
    > 

#### **Parsing(파싱)**

Parsing(파싱)은 데이터를 특정한 **규칙과 구조에 따라 해석하고 분리하여, 의미 있는 단위별로 처리할 수 있도록 만드는 과정입니다.**

예를 들어 보겠습니다.

우리는 다음과 같은 데이터를 받았습니다.

```
STXHelloWorldETXasdasdasSTXAlgorithmETXasdasdaSTXDataPreprocessingETX
```

이 데이터는 `string` 형태로 전달되었으며, 이곳에서 우리는 실제로 읽을 수 있는 영어 단어만을 추출하여 출력해야 한다고 가정해 보겠습니다.

그렇다면 어떻게 해야 할까요?

이 데이터에는 일정한 규칙이 존재합니다.

`STX`는 데이터의 시작을 의미하고, `ETX`는 데이터의 종료를 의미합니다.

따라서 우리는 `STX`를 시작 기준으로 삼고 `ETX`를 종료 기준으로 삼아 데이터를 확인할 수 있습니다.

```
STX | HelloWorld | ETX
STX | Algorithm | ETX
STX | DataPreprocessing | ETX
```

이렇게 일정한 규칙과 구조를 기준으로 데이터를 해석하고, 필요한 정보를 의미 있는 단위로 분리하여 로직에 활용하는 과정을 Parsing이라고 합니다.

- **추가내용:**
    
    > 
    > 
    > 
    > **STX (Start of Text)**
    > 
    > ASCII 제어 문자 중 하나로, 10진수 값은 `2`, 16진수 값은 `0x02`입니다.
    > 
    > 주로 데이터의 시작 지점을 나타내는 용도로 사용하며, 실제 하드웨어 통신이나 특정 통신 프로토콜에서 데이터의 시작을 구분하기 위한 제어 문자로 사용할 수 있습니다.
    > 
    > **ETX (End of Text)**
    > 
    > ASCII 제어 문자 중 하나로, 10진수 값은 `3`, 16진수 값은 `0x03`입니다.
    > 
    > 주로 데이터의 종료 지점을 나타내는 용도로 사용하며, 실제 하드웨어 통신이나 특정 통신 프로토콜에서 데이터의 끝을 구분하기 위한 제어 문자로 사용할 수 있습니다.
    > 
    > C/C++에서는 `char` 타입의 값으로도 표현하여 사용할 수 있으며, 예를 들어 `'\x02'`는 STX, `'\x03'`은 ETX를 나타냅니다.
    > 

#### **Padding**

데이터를 처리하다 보면 기존 데이터만으로는 일정한 규칙을 적용하기 어려운 경우가 있습니다.

이때 우리는 데이터의 앞이나 뒤, 혹은 데이터 사이에 특정 값을 추가하기도 합니다.

예를 들어 경계 조건을 처리하기 위한 값, 매우 큰 값이나 작은 값, 또는 알고리즘에서 자연스럽게 처리될 수 있는 값을 추가하는 방식입니다.

이러한 Padding은 사용되는 목적과 환경에 따라 여러 관점에서 볼 수 있으며, 
여기서는 크게 두 가지 관점으로 나누어 살펴보겠습니다.

**알고리즘에서의 Padding**

알고리즘에서 Padding은 이후 데이터의 처리를 편리하게 만들기 위해 추가적인 값이나 
공간을 확보하는 것입니다.

예시를 보겠습니다. 
저희가 풀었던 문제**“58. Length of Last Word”**를  다시 풀어본다고 생각합시다.
우리는 반복 횟수를 줄이기 위해 아래와 같이 코딩을 했다고 생각합시다.

```cpp

            if ( findEnd == false &&
               ((startIndex = VoidCheck(s[window], window, false)) || 
                (startIndex = VoidCheck(s[window - 1], window - 1, false)) || 
                (startIndex = VoidCheck(s[window - 2], window - 2, false))))
```

위 코드에서의 문제점은 무엇일까요?
그렇습니다. ”문제에서 주는 String인 s가 3의 배수로 주어진다는 보장이 없는데?”
입니다.

그렇다면 정말로 방법은 없는 것일까요?
아닙니다. 이럴 때를 위해 서 저희는 아래 문제의 Padding을 사용하는 것입니다.

```cpp
        if(length % 3 != 0)
        {
        
	        int paddingSize = (3 - length % 3) % 3;
	        
	        for(int loop = 0; loop < paddingSize ; loop = loop + 1)
		      { s.insert(0, " "); }
        
        }
```

네 그렇습니다.
문제에서 String의 길이를 3의 배수로 제공하지 않는다면, 저희가 직접 Padding을 추가하여 길이를 3의 배수로 맞추는 것입니다.

이렇게 한다면 저희는 한번에 3개를 검사할 수 있으며, 이를 통해 더 적은 반복 횟수로 더 빠르게 작동하는 코드를 작성할 수 있게 됩니다.

- **추가내용:**
    
    > 
    > 
    > 
    > 동일한 반복 작업을 수행한다는 조건에서는 반복 횟수가 적을수록 
    > 반복에 필요한 연산량은 감소합니다.
    > 반복 횟수가 적음에도 전체 실행 속도가 느린 경우는 반복문 내부에서 수행하는 
    > 연산량이나 연산 비용이 서로 다른 경우입니다.
    > 

**메모리에서의 Padding과 Alignment**

Padding은 알고리즘에서만 사용되는 것은 아닙니다.

메모리를 관리하는 과정에서도 데이터의 Alignment 조건을 만족하기 위해 Padding이 추가될 수 있습니다.

다음 구조체를 예로 들어보겠습니다.

```
struct Data
{
    char a;
    int b;
};
```

각 자료형의 크기만 생각하면 다음과 같습니다.

```
char = 1 byte
int  = 4 bytes

총 5 bytes
```

하지만 실제 구조체의 크기는 일반적으로 5 bytes가 아닐 수 있습니다.

많은 환경에서 `int`는 4-byte Alignment 조건을 요구합니다.

따라서 `char a`가 저장된 이후 바로 `int b`를 배치하는 것이 아니라, `int b`가 적절한 위치에서 시작할 수 있도록 중간에 Padding이 추가될 수 있습니다.

메모리 구조를 단순하게 표현하면 다음과 같습니다.

```
| a | P | P | P | b | b | b | b |
```

여기서 `P`는 Padding을 의미합니다.

따라서:

```
char a       = 1 byte
Padding      = 3 bytes
int b        = 4 bytes

총           = 8 bytes
```

가 됩니다.

이러한 Padding은 알고리즘에서처럼 데이터를 처리하기 편리하게 만들기 위해 개발자가 직접 추가하는 것이 아니라, 데이터가 메모리에서 적절한 Alignment 조건을 만족할 수 있도록 컴파일러가 추가할 수 있습니다.

또한 구조체가 배열 형태로 사용되는 경우에도 각 구조체의 시작 위치가 Alignment 조건을 유지해야 하므로, 구조체 전체의 크기 역시 Alignment에 영향을 받습니다.

이처럼 메모리에서 데이터가 특정 정렬 조건을 만족하도록 배치하는 것을 Alignment라고 하며, 이를 만족시키기 위해 추가되는 공간을 Padding이라고 합니다.

#### Bit Mask(비트 마스크)

Bit Mask(비트 마스크)란 데이터의 특정 상태나 정보를 **2진수의 각 비트에 대응시켜 표현하고, 이를 통해 데이터를 효율적으로 확인하거나 관리하는 기법**입니다.

사용 예시로는 추가 문제에 등장했던 **「소수 찾기」**를 통해 확인할 수 있습니다.

해당 문제에서는 `n`까지의 소수를 구할 것을 요구합니다.

그 과정에서 에라토스테네스의 체를 활용하는데, 다음과 같이 풀이할 수 있습니다.

```cpp
vector<bool> isPrime(n + 1, true);

isPrime[0] = false;
isPrime[1] = false;

for (int loop = 2; loop * loop <= n; ++loop)
{
    if (!isPrime[loop])
        continue;

    for (int nloop = loop * loop; nloop <= n; nloop += loop)
    {
        isPrime[nloop] = false;
    }
}
```

여기서 `isPrime`은 각 인덱스를 실제 숫자에 대응시키고, 해당 위치의 `bool` 값을 이용하여 **그 숫자가 소수인지 아닌지를 나타냅니다.**

즉,

```
index → 실제 숫자
value → 해당 숫자의 상태
```

와 같이 데이터를 대응시켜 관리하는 것입니다.

예를 들어:

```
isPrime[2] = true  → 2는 소수
isPrime[3] = true  → 3은 소수
isPrime[4] = false → 4는 소수가 아님
isPrime[5] = true  → 5는 소수
```

와 같이 각 데이터의 상태를 `true / false`로 표시할 수 있습니다.

이처럼 **데이터의 특정 상태를 일정한 위치와 값에 대응시켜 표시하고, 이후 로직에서 해당 상태를 빠르게 확인할 수 있도록 만드는 방식**은 Bit Mask를 활용하는 대표적인 형태라고 볼 수 있습니다.

- **추가 내용:**
    
    > 알고리즘 문제 풀이에 등장했던 문제 **「3234. 준환이의 양팔저울」**에서도 Bit Mask를 활용할 수 있습니다.
    발표자의 풀이 **「2. DP + DFS + 비트 마스크(Binary)」** 부분을 참고하는 것을 추천합니다.
    > 

#### Validation(검증)

Validation(검증)은 Data Preprocessing과는 별개의 개념이지만, 실제 시스템에서는 전 처리 과정과 동일하거나 유사한 단계에서 함께 수행되는 경우가 많습니다.

따라서 좁은 의미에서는 Validation과 Data Preprocessing을 구분할 수 있지만, 전체적인 데이터 처리 과정이라는 넓은 관점에서는 Validation을 데이터 전 처리 과정의 일부로 볼 수도 있습니다.

Validation은 입력 받은 Raw Data의 상태와 구성을 기반으로 하여 **데이터의 손상 여부, 유효성, 데이터가 요구하는 작업 등을 확인하는 과정**입니다.

예시를 들어보겠습니다.

우리가 로봇을 제어한다고 가정하고 다음과 같은 통신 데이터를 받았다고 생각해봅시다.

```
STXARM/90/Leg/80/Finger/20ETX
asdasdas
STXARM/40/Leg/50/Finger/70ETX
asdasda
STXARM/100/Leg/60/Finger/50ETX
```

이러한 데이터를 처리하기 위해서는 먼저 데이터를 규칙에 맞게 Parsing하여 각각의 명령 단위를 분리할 수 있습니다.

이후 Validation을 통해 다음과 같은 내용을 확인할 수 있습니다.

1. **데이터의 구조와 위치 확인**
    - 예상되는 시작점과 종료점이 올바른지 확인합니다.
    - 명령어가 요구하는 필드의 개수와 위치가 올바른지 확인합니다.
2. **데이터의 손상 여부 확인**
    - Parsing된 데이터가 명령어의 구조에 맞는지 확인합니다.
    - 각 필드의 형식이 예상한 형태와 일치하는지 확인합니다.
3. **데이터의 유효성 확인**
    - 해당 명령어가 실제로 존재하는 명령인지 확인합니다.
    - 각 명령어가 요구하는 데이터가 올바르게 포함되어 있는지 확인합니다.
    - 전달된 값이 해당 작업에서 허용되는 범위인지 확인합니다.

이러한 검증을 통과한 데이터는 이후 해당 데이터를 처리하는 기능 단위로 전달할 수 있습니다.

예를 들어 다음과 같은 명령어가 정상적인 데이터라고 가정해봅시다.

```
ARM/90/Leg/80/Finger/20
```

Validation이 해당 데이터의 구조와 값이 정상임을 확인했다면, 이후에는 각 데이터를 해당 기능을 처리하는 모듈로 전달할 수 있습니다.

개념적으로 표현하면 다음과 같습니다.

```
ARM/90/Leg/80/Finger/20
        ↓
     Validation
        ↓
   ┌────┼────┐
   ↓    ↓    ↓
 ARM   Leg  Finger
   ↓    ↓    ↓
MoveArm(90)
MoveLeg(80)
MoveFinger(20)
```

여기서 모듈은 특정 데이터를 처리하는 하나의 기능 단위를 의미하며, 간단한 예시에서는 하나의 함수로 표현할 수 있습니다.

즉 Validation은 단순히 데이터가 "맞는지 틀리는지"만 확인하는 것이 아니라, 입력 받은 **Raw Data가 이후 로직에서 신뢰하고 사용할 수 있는 데이터인지 판단**하고**, 검증된 데이터를 어느 처리 과정에서 처리해야 하는지 알리는 역할**도 수행합니다.

#### Quantization(양자화)

**Quantization(양자화)은 데이터의 정밀도 또는 해상도를 제한하여, 연속적이거나 매우 세밀한 값을 일정한 단계의 값으로 대응시키는 기법입니다.**

쉽게 말하면, 데이터가 아무리 세밀하게 표현될 수 있더라도 **우리가 실제로 필요한 수준의 정밀도까지만 사용하여 데이터를 단순화하는 것**입니다.

일상에서도 이러한 예시를 쉽게 찾아볼 수 있습니다.

우리가 사용하는 **30cm 자**를 생각해 봅시다.

자가 1mm 단위의 눈금을 가지고 있다면, 우리는 0.001mm와 같은 더욱 세밀한 단위까지 측정하지 않고 1mm 단위의 정보만을 사용합니다.

체온계 역시 마찬가지입니다.

실제 온도는 매우 세밀하게 변화할 수 있지만, 일반적인 체온계에서는 우리가 필요한 수준의 정밀도에 맞추어 0.1℃ 또는 그와 같은 일정한 단계로 값을 표현합니다.

즉, 실제 데이터가 더 세밀하게 존재하더라도 **목적에 필요한 해상도까지만 표현하고 처리하는 것**입니다.
(여기서 말하는 해상도는 정보의 정밀도를 말합니다.)

**알고리즘에서의 Quantization**

실질적인 알고리즘에서도 비슷한 형태의 데이터 단순화를 수행할 수 있습니다.

저희가 풀었던 **「1859. 백만 장자 프로젝트」**에서 사용한 저의 풀이를 예로 들어보겠습니다.

해당 풀이에서는 모든 날짜를 동일한 단위로 계속 처리하는 대신, 가격의 변화에 따라 **실제로 판매 후보가 되는 날짜를 기준으로 구간을 구성**하였습니다.

즉,

```
모든 날짜

Day 1  Day 2  Day 3  Day 4  Day 5  Day 6  Day 7 ...
  │      │      │      │      │      │      │
  └──────┴──────┴──────┴──────┴──────┴──────┘

                ↓

판매 후보를 기준으로 구간화

[      구간 1      ] [        구간 2        ] [ 구간 3 ]
                   ●                       ●          ●
                 판매                    판매       판매
```

와 같이 데이터를 **목적에 필요한 단위로 단순화**하였습니다.

모든 날짜의 정보를 동일한 해상도로 유지하는 대신, 판매라는 목적에 필요한 지점을 중심으로 데이터를 표현하기 때문에 데이터의 시간적 해상도를 낮춘 것으로 볼 수 있습니다.

다만 이러한 방식은 일반적인 수치 양자화와 완전히 동일한 형태라기보다는, **데이터의 표현 단위와 해상도를 목적에 맞게 줄인다는 양자화의 개념을 알고리즘에 적용한 예시**로 볼 수 있습니다.

- **추가 내용:**
    
    > 「1859. 백만 장자 프로젝트」문제에서 
    발표자의 풀이 중 **「1. 순회」**에서 판매일 병합 과정을 확인해 주세요.
    실제로 판매 일의 해상도를 낮추어 처리하는 것을 보실 수 있습니다.
    > 

**주의할 점**

Quantization은 단순히 **데이터의 최대 범위를 제한하는 것**과는 다릅니다.

예를 들어:

```cpp
Health = FMath::Clamp(Health - DamageAmount, 0.0f, MaxHealth);
```

와 같은 `Clamp`는 데이터가 지정된 범위를 벗어나지 않도록 제한하는 **범위 제한**입니다.

반면 Quantization은:

```
23.4817
23.4818
23.4819

        ↓

23.48
23.48
23.48
```

처럼 **표현 가능한 해상도나 단계 자체를 제한하는 것**에 초점이 있습니다.

따라서 Quantization을 이해할 때는 **"범위를 줄인다"보다는 "해상도 또는 단계의 수를 줄인다"**는 관점으로 이해하는 것이 더 정확합니다.

#### **Compression(압축)**

**Compression(압축)은 데이터가 가지고 있는 규칙성이나 중복성을 이용하여, 동일한 정보를 더 적은 데이터로 표현하는 방식입니다.**

데이터를 압축하는 대표적인 이유는 다음과 같습니다.

1. 저장 공간 절약
2. 데이터 전송량 감소
3. 경우에 따라 데이터 전송 시간 감소

예를 들어 보겠습니다.

저희가 풀었던 알고리즘 문제 **「38. Count and Say」**에서는 **Run-Length Encoding(RLE)** 방식이 등장했습니다.

RLE는 연속해서 등장하는 동일한 데이터를 **반복 횟수와 대상 데이터**로 표현하는 압축 방식입니다.

예를 들어 다음과 같은 문자열이 있다고 생각해 봅시다.

```
33322111333
```

이를 RLE 방식으로 표현하면:

```
333 → 33
22  → 22
111 → 31
333 → 33
```

이 됩니다.

따라서 최종적으로:

```
33322111333

        ↓ RLE

33223133
```

와 같이 표현할 수 있습니다.

이 과정에서 중요한 점은 단순히 동일한 숫자를 찾는 것이 아니라, **연속해서 등장하는 동일한 데이터를 하나의 그룹으로 묶는다는 것**입니다.

이처럼 데이터 내부에 존재하는 **반복성이나 규칙성을 이용하여 동일한 정보를 더 적은 표현으로 나타내는 것이 Compression의 핵심입니다.**

- **추가내용:**
    
    > Compression은 항상 데이터의 길이를 줄여주는 것은 아닙니다.
    예를 들어 반복되는 데이터가 적거나 규칙성이 부족한 경우, 압축 방식에 따라 오히려 데이터의 크기가 증가할 수도 있습니다.
    > 

#### Serialization(직렬화)

**Serialization(직렬화)은 객체나 자료구조처럼 프로그램 내부에서 구조적으로 관리되는 데이터를 저장하거나 전송할 수 있도록 일정한 순서와 형식의 데이터 형태로 변환하는 과정입니다.**

예시를 들어보겠습니다.

기존의 통신 예시에서 다음과 같은 데이터가 존재하였습니다.

```
STXARM/90/Leg/80/Finger/20ETX
```

하지만 해당 데이터는 프로그램 내부에서 처음부터 위와 같은 문자열 형태로 존재했던 것이 아니라, 다음과 같은 구조를 가지고 있었을 수 있습니다.

```
ARM = 90
Leg = 80
Finger = 20
```

또는 코드에서는 다음과 같은 구조체나 객체의 형태로 관리될 수도 있습니다.

```cpp
struct RobotCommand
{
    int ARM;
    int Leg;
    int Finger;
};
```

프로그램 내부에서는 이처럼 여러 개의 변수나 객체의 멤버 변수, 구조체 등의 형태로 데이터를 관리할 수 있습니다.

하지만 이러한 데이터의 구조와 메모리 상태를 그대로 파일이나 다른 프로그램, 혹은 통신 환경으로 전달하는 것은 어려울 수 있습니다.

따라서 데이터를 일정한 규칙과 순서에 따라 전송하거나 저장할 수 있는 형태로 변환하게 됩니다.

```
ARM = 90
Leg = 80
Finger = 20

        ↓ Serialization

STXARM/90/Leg/80/Finger/20ETX
```

이처럼 **프로그램 내부에서 구조적으로 관리되는 데이터를 저장하거나 전송할 수 있는 형태로 변환하는 것이 Serialization입니다**

#### **Deserialization**(역직렬화)

**Deserialization(역직렬화)은 Serialization의 반대 과정으로, 저장되거나 전송된 데이터를 다시 프로그램에서 사용할 수 있는 데이터 구조와 형태로 복원하는 과정입니다.**

앞에서 Serialization을 통해 생성한 다음 데이터를 생각해 보겠습니다.

```
STXARM/90/Leg/80/Finger/20ETX
```

수신한 프로그램은 해당 데이터를 규칙에 따라 해석하여 다음과 같은 형태로 복원할 수 있습니다.

```
ARM = 90

Leg = 80

Finger = 20
```

또는 실제 코드에서는 다음과 같은 구조체나 객체로 데이터를 구성할 수도 있습니다.

```cpp
struct RobotCommand
{
    int ARM;
    int Leg;
    int Finger;
};
```

따라서 전체적인 흐름은 다음과 같이 표현할 수 있습니다.

```
프로그램 내부의 구조화된 데이터

ARM = 90
Leg = 80
Finger = 20

        ↓ Serialization

STXARM/90/Leg/80/Finger/20ETX

        ↓ 저장 또는 전송

STXARM/90/Leg/80/Finger/20ETX

        ↓ Deserialization

ARM = 90
Leg = 80
Finger = 20
```

즉, **Serialization은 데이터를 외부 환경으로 전달하거나 저장하기 위한 형태로 변환하는 과정이며, Deserialization은 해당 데이터를 다시 프로그램 내부에서 사용할 수 있는 형태로 복원하는 과정입니다.**
