####26년 8월 03일

- CH3 C++와 UE로 3D 게임 개발: 

    1.Actor / Component 구조:
    - 
    UE의 최상위 클래스로는 Object가 있으며,
    
    Actor는 해당 오브젝트 레벨(월드)에 배치되거나 스폰(생성)될 수 있는 기본 3D Object 단위이다.
    
    이러한 Actor를 C++로 만들기 위해서는 AActor라는 클래스를 상속할 필요가 있다.

    이렇게 생성된 Actor는 배치하면 보이지도,
    위치 값을 가지지 못 하는데 AActor 자체가 Transform을 직접 제공하는 Component가 아니며, 일반적으로 RootComponent를 통해 Transform의 기준을 갖기 때문이다.

    2.RootComponent / Attachment
    -
    Root Component는 Actor의 최상위 Component이다.

    이러한 Root Component를 사용하는 Actor는 위치, 회전, 크기 등의 Transform 정보를
    기준으로 자식 Component들을 배치하고 관리해야 하기 때문에
    RootComponent로 SceneComponent를 주로 사용한다.

    - SceneComponent를 생성하는 예시 코드:

        SceneRoot = CreateDefaultSubobject<USceneComponent>(TEXT("SceneRoot"));

    SceneComponent는 Transform 정보를 가지며, 다른 Component가 부착될 수 있는 기준점 역할을 한다. 또한 SetupAttachment()와 같이 Component 간 계층 관계를 구성하는 기능에서도 SceneComponent가 부모 기준으로 사용되므로, Transform 계층의 RootComponent로 SceneComponent를 사용하는 경우가 많다.

    다만 모든 Actor가 월드상의 위치 정보를 중요하게 가지는 것은 아니다.
    예를 들어 필드 간 이동을 기반으로 하는 게임에서 배경음악을 재생하는 Actor나
    게임 상태를 관리하는 Manager Actor처럼 기능 수행이 주목적인 Actor는
    위치 정보가 중요하지 않을 수 있으며,
    이러한 경우에는 SceneComponent가 아닌 다른 
    Component를 RootComponent로 사용하기도 한다.

    3.Asset / FObjectFinder
    -

    해당 내용은 단순히 사용법을 외우기보다, UE의 Asset 시스템을 이해한 뒤
    활용하는 것이 좋을 것 같아 작성자가 직접 확인한 내용이다.

    UE의 Asset 시스템은 프로젝트에 존재하는 리소스를
    UE가 사용할 수 있는 Asset으로 관리하고, 이를 UE 에디터에서
    확인하고 참조할 수 있도록 경로와 객체 정보를 관리하는 시스템이다.

    VS2022의 Filter와 비교하면 '가상적인 경로를 통해 리소스를
    정리하고 접근한다'는 점에서 유사하게 생각할 수 있지만,
    Asset 시스템은 단순히 사용자의 가시성이나 정리를 위한 기능이 아니라
    UE가 Asset을 로드하고 관리하기 위한 엔진 내부 시스템이라는 차이가 있다.

    또한 Asset 경로를 이용하면 특정 Asset을 참조할 수 있으며,
    FObjectFinder<T>는 이러한 UE의 Asset/Object 시스템을 이용하여
    지정한 경로의 객체를 찾는 데 사용할 수 있다.


    (아래 내용은 정확한 내용이 아니며 기능을 묘사한 내용이다.)

    FObjectFinder<T>
    
        ↓

    T = 내가 찾고자 하는 Unreal 객체 타입
    
        ↓

    Asset 경로 지정
    
        ↓

    UE의 Asset/Object 시스템을 통해 해당 객체 탐색
    
        ↓

    요구한 타입으로 사용할 수 있는 객체인지 확인
    
        ↓

    찾은 객체를 참조하여 사용
    
    4.Actor Lifecycle
    -
    금일은 Actor Lifecycle에 관련된 함수까지를 주로 배웠다.
    해당 함수는 6 가지로 다음과 같다.

    1. Constructor(생성자)
    2. PostInitializeComponents
    3. BeginPlay
    4. Tick
    5. EndPlay
    6. Destroyed

    일반적인 Actor의 생성 및 플레이 흐름에서는 위 순서에 따라 진행되며, 각 함수는 Actor의 상태에 따라 서로 다른 시점의 초기화 및 처리를 담당한다.
    
    해당 내용에서 작성자는 BeginPlay 부터 생명 주기가 시작 되는 줄 알았으나 생성자부터 생명주기가 시작됨을 알 수 있었다.