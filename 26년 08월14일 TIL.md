# TIL
#### 8월 13일 

1.UE 빌드 단계
- 
 - UBT란?

 Unreal Build Tool의 약자로 UE의 빌드 과정을 관리하는 핵심 도구이다.

 - UBT의 실행 로직:

  UBT는 Target.cs와 Build.cs를 읽고 Target.cs를 기준으로
  
    실행 방법:
     Editor Target
     Game Target
     Client Target
     Server Target

    빌드 구성:
     Debug
     Development
     Shipping

 등을 확인하여 빌드 준비를 하며,
 
 Build.cs를 통해 해당 모듈이 필요로 하는 다른 UE 모듈에 대한 의존성을 선언하고, UBT가 이를 바탕으로 빌드 환경을 구성한다.

 - UHT란?

    UnrealHeaderTool로 
    UHT는 UCLASS, UPROPERTY, UFUNCTION 등의 UE 리플렉션 관련 선언을 분석하고,
    이를 바탕으로 generated.h 등의 Generated Code를 생성한다.

    이후 C++ 전처리 과정에서 generated.h가 #include되고,
    GENERATED_BODY() 등의 매크로가 Generated Code와 연결되어
    UE 리플렉션 시스템에 필요한 코드가 최종 컴파일 대상에 포함된다.
