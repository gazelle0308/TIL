# TIL
#### 8월 18일 

1.CDO
- 
- CDO란?

    Class Default Object라는 의미로, UClass의 기본 프로퍼티 값을 가지고 있는 원형에 가까운 객체라고 할 수 있다.

    이러한 CDO의 의의는 자신이 만든 C++ 오브젝트를 BP로 계승하거나, Actor를 화면에 배치할 때 의미를 가지게 된다.
    CDO는 이러한 상황에서 해당 BP 오브젝트나 새롭게 생성되는 오브젝트의 기본값을 구성하는 기준이 되어준다.

    이렇게 들으면 CDO의 아래에 동일 타입의 다른 오브젝트들이
    부모 관계 또는 종속 관계를 맺는 것처럼 느껴질 수 있지만 실제로 그러하지는 않다.

    CDO는 화면에 배치되어 플레이되는 일반적인 Actor 인스턴스는 아니지만,
    C++/UE 객체 시스템 관점에서는 실제로 존재하는 객체이다.
    그리고 다른 오브젝트가 생성될 때 CDO의 기본 프로퍼티 값이
    해당 오브젝트의 초기값을 구성하는 기준으로 사용된다.

    따라서 CDO와 실제 오브젝트들이 부모-자식 또는 종속 관계로 존재한다고 이해하기보다는,
    CDO는 해당 UClass의 기본 상태를 정의하는 원형 객체라고 이해하는 것이 적절하다.

2.알고리즘 학습
-
- 금일 학습한 문제: https://leetcode.com/problems/spiral-matrix-ii/description/?utm_source=chatgpt.com

작성 코드 :
----------------------------------------
    class Solution {
    public:

        void AxisSet(bool axis, int& plusAxis, int& minusAxis)
        {

            if(axis)
            { plusAxis = plusAxis + 1; }
            else
            { minusAxis = minusAxis + 1; }

        }

        bool AxisChangeCheck(int plusAxis, int minusAxis, int maxAxis, int minAxis)
        { return plusAxis - minusAxis == minAxis || plusAxis - minusAxis == maxAxis; }

        void SetMaxLine(bool axis, int& Max, int& Min)
        { 

            if (axis)
            { Max = Max - 1; }
            else
            { Min = Min + 1; }

        }

        vector<vector<int>> generateMatrix(int n) 
        {

            int MaxRight = n - 1;
            int MaxLeft = 0;

            int MaxDown = n - 1;
            int MaxUp = 0;

            int right = 0;
            int left = 0;
            int up = 0;
            int down = 0;
            int isOutLine = 4;

            bool isRight = true;
            bool isDown = false;
            bool isWidth = false;

            vector<vector<int>> Matrix(n, vector<int>(n, 0));

            for (int squre = 1; squre <= n * n; squre = squre + 1)
            {

                int width = right - left;
                int height = down - up;

                Matrix[height][width] = squre;

                if (AxisChangeCheck(down, up, MaxDown, MaxUp) && AxisChangeCheck(right, left, MaxRight, MaxLeft))
                { 
                    
                    if(isOutLine) { isOutLine = isOutLine - 1; }

                    isWidth = !isWidth;
                    if (!isWidth)
                    { 

                        if (!isOutLine) 
                        {  SetMaxLine(isDown, MaxDown, MaxUp); }
                        isRight = !isRight; 

                    }
                    else
                    {
                        
                        if (!isOutLine) 
                        { SetMaxLine(isRight, MaxRight, MaxLeft); } 
                        isDown = !isDown;

                    }

                }

                if (isWidth)
                { AxisSet(isRight, right, left); }
                else
                { AxisSet(isDown, down, up); }

            }

            return Matrix;

        }

    };
-------------------------------------------------------------
- 해당 코드는 2차원 Matrix에 나선형으로 주어진 값 N의 제곱수 까지를 채우는 문제이다.

    해당 문제를 작성하며 작성자는 수식의 이미지화에 어려움을 겪어 여러 로직 에러를 겪었는데 이에 대한 해결법을 문제를 해결하고 난 뒤 알 수 있었다.

    다음은 다른 작성자가 작성한 코드이다.
-------------------------------------------------------------
    int dy[4]{0, 1, 0, -1}; // row. 우하좌상
    int dx[4]{1, 0, -1, 0}; // column. 우하좌상
    int dIndex = 0;


    ny = y + dy[dIndex];
    nx = x + dx[dIndex];
    
                // matrix의 범위 안에 있고 채워지지 않은 칸인지 확인
    if (0 <= ny && ny < n && 0 <= nx && nx < n && matrix[ny][nx] == 0)
    {
        y = ny;
        x = nx;
    }
    else
    {
            // 해당 안 하면 방향 전환
        dIndex = (dIndex + 1) % 4;
        y += dy[dIndex];
        x += dx[dIndex];
    }
-------------------------------------------------------
 - 이와 같이 Matrix의 경계선의 이동을 직접 관리하는 방식보다, 다음 위치가 유효한지와 이미 방문한 위치인지를 기준으로 이동 방향을 결정하는 방식이면 로직적으로 작성자가 해야할 고민도 줄어들며 로직적으로 완성도 또한 늘어난다.

 - 이를 통하여 작성자는 Matrix의 탐색 방법에 대하여 한 가지 방법을 더 배울 수 있었으며, 기존의 방식도 완전히 배제하지는 아니하며 수식의 이미지화와 간단하고 명료한 로직을 작성하는데 최선을 다할 것이다.


 3.게임 개발의 방향성
 -

 해당 내용은 CH3 과제를 진행하던 중간에 유저의 조작감과 개발자의 마인드에 대하여 배울 수 있었던 사례이다.

 	if (!Controller) { return; }

	const FVector2D MoveInput = value.Get<FVector2D>();
	FVector Forward = GetActorForwardVector();
	FVector Right = GetActorRightVector();

	Forward.Z = 0.0f; // 지면 이동 시 Z축 방향의 이동을 제거하기 위한 값
	Right.Z = 0.0f;   // 지면 이동 시 Z축 방향의 이동을 제거하기 위한 값

	Forward.Normalize(); // Z값의 제거에 의해 깨진 균형 값을 맞춰주기 위한 Normalize
	Right.Normalize(); // Z값의 제거에 의해 깨진 균형 값을 맞춰주기 위한 Normalize
	

	// Normalize():
	// 벡터의 방향은 유지하면서 벡터의 길이를 1로 만든다.
 

 - 해당 코드는 유저의 조작감을 고려하여 작성한 코드이다.
이 코드는 내가 직접 유저가 되어 캐릭터를 조작한다고 가정했을 때, 자연스러운 조작감을 위해 필요하다고 판단하여 구현한 코드로 다음과 같은 생각을 기반으로 하였다.

 1. 유저는 무의식적으로 상하 시선 이동과 지상 이동은 별개라고 인식한다.
 2. 따라서 지상에서는 고개의 움직임과 관계없이 이동 속도가 일정할 것이라고 기대한다.
 3. 그러므로 Z값 제거로 인해 벡터의 길이가 달라지는 해당 상황에서는,
 4. Normalize()를 통해 방향은 유지하면서 벡터의 길이를 1로 정규화하여
 5. 시선의 상하 움직임에 따라 지상 이동 속도가 달라지지 않도록 구현하였다.

    즉, 단순히 기능을 구현하는 것에서 끝나는 것이 아니라 유저가 실제로 어떻게 느끼고 조작할지를 함께 고민하며 개발하는 것이 게임 개발자로서 좋은 방향성 중 하나라고 느꼈다.

- 주의 사항:
    
    내가 생각하는 "게임이라면 당연한 것"을 구현에 잘 녹이는 것은 중요하다.
    
    다만 내가 당연하다고 생각하는 것이 항상 정답이라고 판단하지 않고, 실제 플레이 경험과 목적에 맞는지 검증하며 필요한 것만 구현하는 것이 중요하다.