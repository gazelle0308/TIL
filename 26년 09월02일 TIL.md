# TIL
## 9월 02일 

### 1. 데이터 전 처리

J. AoS / SoA
AoS(Array of Structures)와 SoA(Structure of Arrays)는 동일한 데이터를 어떤 구조로 배치하고, 어떤 단위로 접근할 것인지에 대한 데이터 구성 방식입니다.
데이터를 객체 단위로 묶을 것인지, 데이터의 종류별로 묶을 것인지가 핵심입니다.
AoS(Array of Structures)
AoS는 하나의 데이터 묶음을 구조체로 만들고, 해당 구조체를 배열이나 vector 등에 저장하는 방식입니다.
struct Player
{
    int hp;
    int mp;
    int attack;
};

vector<Player> players(100);
​
데이터의 형태를 단순하게 표현하면 다음과 같습니다.
Player 1 → hp / mp / attack
Player 2 → hp / mp / attack
Player 3 → hp / mp / attack
...
​
AoS 구조는 개별 데이터 묶음에 접근하기 용이하다는 특징이 있습니다.
따라서 하나의 대상에 대해 여러 데이터를 함께 조회하거나 처리해야 하는 경우 자연스럽게 사용할 수 있습니다.
예를 들어:
players[0].hp
players[0].mp
players[0].attack
​
와 같이 하나의 Player에 대한 여러 정보를 함께 사용하는 경우에 적합합니다.
SoA(Structure of Arrays)
SoA는 AoS와 반대로 데이터의 종류별로 배열이나 vector를 분리하여 저장하는 방식입니다.
struct Players
{
    vector<int> hp;
    vector<int> mp;
    vector<int> attack;
};

Players players;

players.hp.resize(100);
players.mp.resize(100);
players.attack.resize(100);
​
데이터의 형태를 단순하게 표현하면 다음과 같습니다.
hp     → hp / hp / hp / hp / ...
mp     → mp / mp / mp / mp / ...
attack → attack / attack / attack / attack / ...
​
SoA 구조는 동일한 종류의 데이터를 연속적으로 접근하기 용이하다는 특징이 있습니다.
예를 들어 모든 캐릭터의 HP를 일괄적으로 수정하거나 조사해야 한다면:
for (int index = 0; index < 100; ++index)
{
    hp[index] -= 10;
}
​
와 같이 특정 데이터만을 대상으로 처리할 수 있습니다.
AoS와 SoA의 차이
두 방식은 서로 다른 데이터를 저장하는 것이 아니라 같은 데이터를 서로 다른 방식으로 배치하는 것입니다.
AoS
Player 1 → HP / MP / Attack
Player 2 → HP / MP / Attack
Player 3 → HP / MP / Attack

        ↓

개별 객체 단위의 접근에 유리
​
SoA
HP     → HP / HP / HP
MP     → MP / MP / MP
Attack → Attack / Attack / Attack

        ↓

특정 데이터의 일괄 접근에 유리
​
따라서 어느 한 방식이 항상 더 좋은 것이 아니라, 이후 데이터를 어떤 방식으로 접근하고 처리할 것인지에 따라 적절한 구조를 선택하는 것이 중요합니다.