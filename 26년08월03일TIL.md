####26년 8월 03일

- CH2 협업 프로젝트: 
    1. 금일을 기준으로 프로젝트 내 담당 기능의 구현을 완료하였으며, 정상적으로 동작함을 확인하였다.
    2. 이후에는 팀원들의 코드와 연계되는 부분의 디버깅과, 구현한 코드의 리팩토링을 진행할 예정이다.


- 8순환 Include와 Template 특수화로 인한 의존성 문제 해결:

    - 8월 2일, Circular Include와 Template Specialization 과정에서 객체의 전체 정의가 필요한 상황이 발생하였다.

    - 이번 문제는 **템플릿 기반 공통 인터페이스(Effect<T>)**와 이를 사용하는 객체(Player, Monster) 사이의 상호 의존성(Coupling) 때문에 발생하였다.

    - Effect<T>는 타입별 특수화(Effect<Player>, Effect<Monster>)를 사용하므로, 전방 선언(Forward Declaration)만으로는 충분하지 않았으며, 특수화를 구현하기 위해서는 대상 객체의 전체 정의가 필요하였다.

    - 반대로 Player와 Monster는 자신의 생명주기 동안 Effect 객체를 사용해야 했기 때문에, Effect와의 참조 관계 또한 고려해야 했다.

    - 이를 해결하기 위해 다음과 같은 구조를 적용하였다.

        - Player는 게임 전체에서 하나만 존재하는 객체이므로, Effect<Player> 역시 Singleton을 이용하여 Player와 동일한 생명주기를 갖도록 설계하였다.

        - Monster는 전투마다 생성·소멸되는 객체이므로 Singleton을 사용하지 않고, 생성자를 통해 Effect<Monster>를 생성하여 짧은 생명주기를 유지하도록 설계하였다.

    - 이를 통해 순환 Include 문제를 해결하면서도, 객체의 생명주기에 맞는 Effect 관리 구조를 구성할 수 있었다.


- 래퍼 함수의 학습:

    - 작성자는 프로젝트 진행 도중 팀원 들과의 협업을 위해 멤버 포인터로 가져온 객체의 함수를 쉽게 불러 올 수 있도록 구현하였는데 이 방식을 래퍼 함수라고 한다는 것을 알았다.

    - 예시 코드:

    class Player {
    public:
        void Tick() {
            effect.Tick();
        }

        void ClearEffect() {
            effect.ClearEffect();
        }

    private:
        Effect<Player> effect;
    };
